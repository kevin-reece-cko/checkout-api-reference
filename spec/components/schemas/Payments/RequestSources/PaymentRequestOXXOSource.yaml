###
# Warning: this file was auto generated from OpenAPI specs using 'npm run sync-generated-specs'. Do not manually edit. 
###
type: object
description: 'OXXO Source'
allOf:
  -
    $ref: '#/components/schemas/PaymentRequestSource'
  -
    type: object
    required:
      - country
      - payer
    properties:
      integration_type:
        enum:
          - redirect
        type: string
        description: "The type of the integration.   \nCurrently only `redirect`, for which a redirect url is returned,  which the merchant redirects the customer to, to continue payment"
      country:
        maxLength: 2
        enum:
          - MX
        type: string
        description: 'User''s country code. ISO 3166-1 alpha-2 code.'
      payer:
        required:
          - name
          - email
          - document
        type: object
        properties:
          name:
            maxLength: 100
            type: string
            description: 'User''s full name.'
          email:
            maxLength: 100
            type: string
            description: 'User’s email address.'
          birth_date:
            pattern: ^\d\d-\d\d-\d\d\d\d$
            type: string
            description: 'User’s birthdate (DD-MM-YYYY).'
          phone:
            maxLength: 20
            type: string
            description: 'User’s phone.'
          document:
            maxLength: 100
            type: string
            description: 'User’s personal identification number in Mexico (CURP).'
        description: 'Payer object'
      description:
        maxLength: 200
        type: string
        description: 'Payment description.'
