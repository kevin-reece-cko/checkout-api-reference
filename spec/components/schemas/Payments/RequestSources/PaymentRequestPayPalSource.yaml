type: object
required:
  - reference
allOf:
  - $ref: "#/components/schemas/PaymentRequestSource"
  - type: object
    properties:
      reference:
        type: string
        description: A unique payment reference identifier per successful payment