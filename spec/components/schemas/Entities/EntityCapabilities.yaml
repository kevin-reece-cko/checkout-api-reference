type: object
title: Capabilities
properties:
  payments:
    type: object
    description: "Payment related capabilities of a sub-entity"
    properties:
      enabled:
        type: boolean
        description: True if payments are enabled
    example:
      enabled: false