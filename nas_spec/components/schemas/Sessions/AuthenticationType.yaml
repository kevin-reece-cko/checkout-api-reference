type: string
enum:
  - regular
  - recurring
  - instalment
  - maintain_card
  - add_card
description: Indicates the type of payment this session is for. Please note the spelling of `instalment` consists of one `l` only.
example: regular
default: regular
