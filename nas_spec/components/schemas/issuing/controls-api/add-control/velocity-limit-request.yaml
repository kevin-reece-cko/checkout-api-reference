type: object
allOf:
  - $ref: '#/components/schemas/add-control-request'
  - type: object
    properties:
      velocity_limit:
        $ref: '#/components/schemas/VelocityLimit'
required:
  - velocity_limit
