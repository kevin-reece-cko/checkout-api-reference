type: object
description: Authorization Response.
required:
  - status
properties:
  status:
    type: string
    description: The transaction's status.
    enum:
      - Authorized
      - Declined
    example: Authorized